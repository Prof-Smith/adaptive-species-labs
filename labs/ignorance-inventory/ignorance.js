const canvas=document.getElementById('ignoranceCanvas'),ctx=canvas.getContext('2d');const ids=['known','assumption','impact','learnability','urgency','monitoring','diversity','experiment'];let points=[];function v(id){return parseInt(document.getElementById(id).value)}function labels(){ids.forEach(id=>document.getElementById(id+'Label').textContent=document.getElementById(id).value)}function initPoints(){points=[];let types=['known','assumed','unknown','blind','learn'];for(let i=0;i<34;i++)points.push({type:types[i%types.length],x:70+Math.random()*580,y:85+Math.random()*330,r:8+Math.random()*10})}function color(t){return t==='known'?'#2563eb':t==='assumed'?'#f59e0b':t==='unknown'?'#94a3b8':t==='blind'?'#dc2626':'#16a34a'}function calc(){let known=v('known'),assumption=v('assumption'),impact=v('impact'),learnability=v('learnability'),urgency=v('urgency'),monitoring=v('monitoring'),diversity=v('diversity'),experiment=v('experiment');let risk=Math.round(.30*assumption+.28*impact+.18*urgency+.14*(100-monitoring)+.10*(100-diversity)-.16*known);let learn=Math.round(.36*learnability+.28*experiment+.20*monitoring+.16*diversity);let priority=Math.round(.45*impact+.25*urgency+.20*learnability+.10*(100-known));risk=Math.max(0,Math.min(100,risk));learn=Math.max(0,Math.min(100,learn));priority=Math.max(0,Math.min(100,priority));return{known,assumption,impact,learnability,urgency,monitoring,diversity,experiment,risk,learn,priority}}function level(x){return x<40?'Low':x<70?'Watch':'High'}function draw(){labels();let c=calc();ctx.clearRect(0,0,720,520);let grad=ctx.createLinearGradient(0,0,720,520);grad.addColorStop(0,'#eef5ff');grad.addColorStop(1,'#fff8ed');ctx.fillStyle=grad;ctx.fillRect(0,0,720,520);ctx.strokeStyle='rgba(31,78,121,.18)';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(80,420);ctx.lineTo(640,420);ctx.moveTo(80,420);ctx.lineTo(80,80);ctx.stroke();ctx.fillStyle='#1f2937';ctx.font='bold 13px Arial';ctx.fillText('Lower impact',82,450);ctx.fillText('Higher impact',560,450);ctx.save();ctx.translate(40,310);ctx.rotate(-Math.PI/2);ctx.fillText('More knowable',0,0);ctx.restore();ctx.save();ctx.translate(40,145);ctx.rotate(-Math.PI/2);ctx.fillText('Less knowable',0,0);ctx.restore();points.forEach(p=>{let px=p.x,py=p.y;if(p.type==='blind'){px=520+c.impact*.8*Math.random();py=115+(100-c.diversity)*1.1*Math.random()}if(p.type==='learn'){px=150+c.learn*.9*Math.random();py=340-c.learnability*2.1*Math.random()}ctx.globalAlpha=p.type==='blind'?0.55+c.risk/210:0.86;ctx.beginPath();ctx.arc(Math.min(655,px),Math.max(70,Math.min(430,py)),p.r,0,Math.PI*2);ctx.fillStyle=color(p.type);ctx.fill();ctx.fillStyle='white';ctx.font='bold 10px Arial';ctx.textAlign='center';ctx.fillText(p.type==='known'?'K':p.type==='assumed'?'A':p.type==='unknown'?'?':p.type==='blind'?'!':'L',Math.min(655,px),Math.max(70,Math.min(430,py))+3);ctx.globalAlpha=1});ctx.fillStyle='rgba(255,255,255,.88)';ctx.fillRect(18,18,278,76);ctx.fillStyle='#1f2937';ctx.textAlign='left';ctx.font='bold 13px Arial';ctx.fillText('Map unknowns by impact and knowability',30,42);ctx.fillText('Red means high-impact blind spot pressure',30,62);ctx.fillText('Green means learnable unknowns',30,82);metrics(c)}function metrics(c){document.getElementById('riskMetric').textContent=level(c.risk);document.getElementById('learnMetric').textContent=c.learn;document.getElementById('priorityMetric').textContent=c.priority;document.getElementById('ignoranceFill').style.width=c.risk+'%';let diag=c.risk>70?'Ignorance risk is high. Assumptions and high-impact unknowns are outrunning monitoring and frame diversity.':c.learn>70?'The system has strong learning capacity. Unknowns are not eliminated, but they are being converted into tests and triggers.':'The inventory is usable, but one or two unknowns should be prioritized before commitment.';document.getElementById('diagnosisText').textContent=diag;document.getElementById('warningText').textContent=c.risk>65?'Do not treat assumptions as facts. Name the unknown that could most change the decision and build either a test or a trigger.':c.learn<50?'Learning capacity is thin. Add a small experiment, outside perspective, or monitoring trigger.':'Ignorance is visible enough to manage. Keep the inventory updated after action.';document.getElementById('ignoranceChips').innerHTML=`<span class="ignorance-chip">Risk ${c.risk}</span><span class="ignorance-chip">Learning ${c.learn}</span><span class="ignorance-chip">Priority ${c.priority}</span>`;let items=[['Assumption load',c.assumption],['Unknown impact',c.impact],['Monitoring quality',c.monitoring],['Frame diversity',c.diversity]];document.getElementById('unknownList').innerHTML=items.map(([n,val])=>`<div class="unknown-item"><b>${n}</b><p class="small">Current value: ${val}</p></div>`).join('');document.getElementById('importantText').textContent=c.impact>65?'The highest-impact unknown should be tested or monitored before commitment.':'The unknowns appear lower-impact, but still need triggers.';document.getElementById('testText').textContent=c.learnability>60?'Run a small experiment before scaling the decision.':c.diversity<50?'Bring in a different frame or outside comparison before testing.':'Use a decision journal and define an update trigger.';document.getElementById('triggerText').textContent=c.monitoring<50?'Create a visible signal that forces review after action.':'Set a threshold that would require revising the decision.'}ids.forEach(id=>document.getElementById(id).addEventListener('input',draw));document.getElementById('runBtn').onclick=draw;document.getElementById('blindspotBtn').onclick=()=>{let s={known:35,assumption:82,impact:88,learnability:35,urgency:80,monitoring:25,diversity:20,experiment:28};ids.forEach(id=>document.getElementById(id).value=s[id]);draw()};document.getElementById('learningBtn').onclick=()=>{let s={known:60,assumption:45,impact:75,learnability:78,urgency:50,monitoring:80,diversity:75,experiment:82};ids.forEach(id=>document.getElementById(id).value=s[id]);draw()};document.getElementById('resetBtn').onclick=()=>{let s={known:55,assumption:60,impact:70,learnability:50,urgency:55,monitoring:45,diversity:40,experiment:45};ids.forEach(id=>document.getElementById(id).value=s[id]);draw()};initPoints();draw();

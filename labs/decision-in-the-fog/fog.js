const canvas=document.getElementById('fogCanvas'),ctx=canvas.getContext('2d');let chosen=null,result=null;const scenarios={career:'Choose whether to take an uncertain opportunity, stay with the current path, or delay for more information.',investment:'Choose whether to invest, hold cash, or stage the commitment.',program:'Choose whether to launch, pilot, or postpone a new academic program.',ai:'Choose whether to rely heavily on AI, use AI as support, or work mostly unaided.'};function v(id){return parseInt(document.getElementById(id).value)}function setLabels(){['clarity','pressure','stakes','optionality','confidence'].forEach(id=>document.getElementById(id+'Label').textContent=document.getElementById(id).value);document.getElementById('scenarioText').textContent=scenarios[document.getElementById('scenario').value]}function processScore(){let clarity=v('clarity'),pressure=v('pressure'),stakes=v('stakes'),opt=v('optionality'),conf=v('confidence');let score=50+.28*clarity+.18*opt-.16*pressure-.08*stakes-Math.max(0,conf-clarity)*.22;if(document.getElementById('known').value.trim())score+=5;if(document.getElementById('assumed').value.trim())score+=5;if(document.getElementById('unknown').value.trim())score+=6;if(document.getElementById('update').value.trim())score+=8;return Math.max(0,Math.min(100,Math.round(score)))}function draw(){setLabels();let clarity=v('clarity'),pressure=v('pressure'),stakes=v('stakes'),opt=v('optionality'),conf=v('confidence');ctx.clearRect(0,0,720,520);let grd=ctx.createLinearGradient(0,0,720,520);grd.addColorStop(0,'#eef5ff');grd.addColorStop(1,'#fff8ed');ctx.fillStyle=grd;ctx.fillRect(0,0,720,520);ctx.fillStyle='rgba(148,163,184,'+(0.65-clarity/180)+')';for(let i=0;i<140;i++){let x=(i*73)%720,y=(i*37)%520,r=24+((i*9)%45);ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fill()}let paths=[{id:'A',x:170,y:380,c:'#16a34a',label:'Commit'},{id:'B',x:360,y:280,c:'#f59e0b',label:'Stage'},{id:'C',x:550,y:380,c:'#2563eb',label:'Wait'}];ctx.lineWidth=8;paths.forEach(p=>{ctx.strokeStyle=p.c;ctx.globalAlpha=.8;ctx.beginPath();ctx.moveTo(360,470);ctx.quadraticCurveTo(p.x,420,p.x,p.y);ctx.stroke();ctx.globalAlpha=1;ctx.fillStyle=p.c;ctx.beginPath();ctx.arc(p.x,p.y,34,0,Math.PI*2);ctx.fill();ctx.fillStyle='white';ctx.font='bold 18px Arial';ctx.textAlign='center';ctx.fillText(p.id,p.x,p.y+6);ctx.fillStyle='#1f2937';ctx.font='bold 13px Arial';ctx.fillText(p.label,p.x,p.y+55)});function marker(x,y,c,t){ctx.fillStyle=c;ctx.beginPath();ctx.arc(x,y,12,0,Math.PI*2);ctx.fill();ctx.fillStyle='white';ctx.font='bold 11px Arial';ctx.fillText(t,x,y+4)}for(let i=0;i<Math.round(clarity/16)+2;i++)marker(90+i*56,94+(i%2)*35,'#2563eb','K');for(let i=0;i<Math.round((100-clarity)/20)+2;i++)marker(520+(i%3)*45,92+Math.floor(i/3)*39,'#94a3b8','?');for(let i=0;i<Math.round(stakes/22)+1;i++)marker(105+i*70,230+(i%2)*40,'#dc2626','!');for(let i=0;i<Math.round(opt/25)+1;i++)marker(520+i*42,250+(i%2)*44,'#16a34a','+');for(let i=0;i<Math.round(conf/28)+1;i++)marker(335+i*38,135+(i%2)*32,'#7c3aed','U');ctx.fillStyle='#102a43';ctx.font='bold 18px Arial';ctx.textAlign='left';ctx.fillText('You are here',305,492); if(chosen){ctx.fillStyle='rgba(197,90,17,.16)';let p=paths.find(x=>x.id===chosen);ctx.beginPath();ctx.arc(p.x,p.y,50,0,Math.PI*2);ctx.fill()}}function choose(c){chosen=c;result=null;clearRooms();draw()}function clearRooms(){['good-good','good-bad','bad-good','bad-bad'].forEach(r=>document.getElementById('room-'+r).classList.remove('active'));document.getElementById('outcomeMetric').textContent='—';document.getElementById('roomMetric').textContent='—';document.getElementById('processMetric').textContent=processScore()}function reveal(){if(!chosen)return;let score=processScore();let clarity=v('clarity'),pressure=v('pressure'),stakes=v('stakes'),opt=v('optionality');let base=50+(chosen==='B'?8:0)+(chosen==='C'?opt*.12-pressure*.08:0)+(chosen==='A'?stakes*.08:0)+clarity*.12-Math.random()*55;let good=base>45;let proc=score>=60;let room=(proc?'good':'bad')+'-'+(good?'good':'bad');document.getElementById('room-'+room).classList.add('active');document.getElementById('processMetric').textContent=score;document.getElementById('outcomeMetric').textContent=good?'Good':'Bad';document.getElementById('roomMetric').textContent=room.replace('-',' / ');draw()}document.querySelectorAll('[data-choice]').forEach(b=>b.onclick=()=>choose(b.dataset.choice));document.getElementById('revealBtn').onclick=reveal;document.getElementById('resetBtn').onclick=()=>{chosen=null;result=null;clearRooms();draw()};['scenario','clarity','pressure','stakes','optionality','confidence'].forEach(id=>document.getElementById(id).addEventListener('input',()=>{clearRooms();draw()}));document.querySelectorAll('textarea').forEach(t=>t.addEventListener('input',()=>{document.getElementById('processMetric').textContent=processScore()}));setLabels();clearRooms();draw();

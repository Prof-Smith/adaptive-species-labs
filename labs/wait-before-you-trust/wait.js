const canvas=document.getElementById('waitCanvas'),ctx=canvas.getContext('2d');const ids=['delegation','complexity','inspection','assumptions','transfer','pressure','accuracy','feedback'];function v(id){return parseInt(document.getElementById(id).value)}function labels(){ids.forEach(id=>document.getElementById(id+'Label').textContent=document.getElementById(id).value)}function calc(){let delegation=v('delegation'),complexity=v('complexity'),inspection=v('inspection'),assumptions=v('assumptions'),transfer=v('transfer'),pressure=v('pressure'),accuracy=v('accuracy'),feedback=v('feedback');let workflow=Math.max(0,Math.min(100,100-Math.abs(delegation-55)*.85-pressure*.12+inspection*.10));let output=Math.round(.42*accuracy+.22*delegation+.14*inspection+.12*assumptions+.10*feedback-.12*complexity);let skill=Math.round(.36*transfer+.24*inspection+.20*assumptions+.12*feedback-.18*delegation+.10*complexity);let risk=Math.round(.30*delegation+.22*pressure+.20*complexity+.18*(100-inspection)+.14*(100-assumptions)-.18*accuracy);output=Math.max(0,Math.min(100,output));skill=Math.max(0,Math.min(100,skill));risk=Math.max(0,Math.min(100,risk));let waitScore=Math.round((workflow+assumptions+inspection+transfer)/4);return{delegation,complexity,inspection,assumptions,transfer,pressure,accuracy,feedback,workflow,output,skill,risk,waitScore}}function level(x){return x<40?'Low':x<70?'Watch':'High'}function draw(){labels();let c=calc();ctx.clearRect(0,0,720,520);let grad=ctx.createLinearGradient(0,0,720,520);grad.addColorStop(0,'#eef5ff');grad.addColorStop(1,'#fff8ed');ctx.fillStyle=grad;ctx.fillRect(0,0,720,520);let human=120,ai=600,mid=360;ctx.strokeStyle='#dbeafe';ctx.lineWidth=8;ctx.beginPath();ctx.moveTo(human,260);ctx.lineTo(ai,260);ctx.stroke();function node(x,y,r,color,label,sub){ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fillStyle=color;ctx.fill();ctx.fillStyle='white';ctx.font='bold 15px Arial';ctx.textAlign='center';ctx.fillText(label,x,y-2);ctx.font='11px Arial';ctx.fillText(sub,x,y+17)}node(human,260,54,'#2563eb','Human','judgment');node(ai,260,54,'#c55a11','AI','output');let riskR=25+c.risk*.7;ctx.globalAlpha=.20;ctx.fillStyle='#dc2626';ctx.beginPath();ctx.arc(mid,260,riskR,0,Math.PI*2);ctx.fill();ctx.globalAlpha=1;node(mid,260,42,c.risk>65?'#dc2626':'#16a34a','WAIT',c.waitScore);let transferY=405;ctx.strokeStyle='#16a34a';ctx.lineWidth=6;ctx.beginPath();ctx.moveTo(ai,320);ctx.quadraticCurveTo(mid,transferY,human,320);ctx.stroke();ctx.fillStyle='#16a34a';ctx.beginPath();ctx.arc(mid,transferY,18+c.skill*.16,0,Math.PI*2);ctx.fill();ctx.fillStyle='white';ctx.font='bold 12px Arial';ctx.textAlign='center';ctx.fillText('Skill',mid,transferY+4);let delegationWidth=8+c.delegation*.18;ctx.strokeStyle='rgba(197,90,17,.8)';ctx.lineWidth=delegationWidth;ctx.beginPath();ctx.moveTo(human+54,260);ctx.lineTo(ai-54,260);ctx.stroke();ctx.fillStyle='rgba(255,255,255,.88)';ctx.fillRect(18,18,258,76);ctx.fillStyle='#1f2937';ctx.textAlign='left';ctx.font='bold 13px Arial';ctx.fillText('Thicker orange = more delegation',30,42);ctx.fillText('Red halo = uninspected risk',30,62);ctx.fillText('Green arc = learning transfer',30,82);metrics(c)}function metrics(c){document.getElementById('qualityMetric').textContent=c.output;document.getElementById('riskMetric').textContent=level(c.risk);document.getElementById('skillMetric').textContent=c.skill;document.getElementById('waitFill').style.width=c.risk+'%';let bars=[['Workflow',c.workflow],['Assumptions',c.assumptions],['Inspection',c.inspection],['Transfer',c.transfer]];bars.forEach(([name,val])=>{document.getElementById('bar'+name).style.width=Math.round(val)+'%';document.getElementById('pct'+name).textContent=Math.round(val)+'%'});let diag=c.risk>70?'AI is producing faster than the human workflow can inspect. Reliance risk is high.':c.skill<45?'Output may improve, but transfer is weak. The user may not be learning much from the workflow.':c.output>70&&c.skill>60?'AI is extending judgment while preserving inspection and transfer.':'The workflow is useful but fragile. Strengthen one WAIT checkpoint.';document.getElementById('diagnosisText').textContent=diag;document.getElementById('warningText').textContent=c.risk>65?'Slow down the handoff. Name assumptions, inspect claims, and require a transfer step before accepting the output.':c.skill<50?'Add a teach-back, revision, or explanation step so the user carries skill forward.':'WAIT discipline is protecting the workflow. Keep AI in the loop, not over the loop.';document.getElementById('waitChips').innerHTML=`<span class="wait-chip">WAIT ${c.waitScore}</span><span class="wait-chip">Output ${c.output}</span><span class="wait-chip">Risk ${c.risk}</span>`;document.getElementById('strengthText').textContent=c.output>75?'High productivity and polish.':c.skill>70?'Strong learning transfer.':'Balanced support, but not yet excellent.';document.getElementById('failureText').textContent=c.risk>70?'Automation complacency':c.delegation>75&&c.skill<50?'Skill atrophy':c.inspection<45?'Unverified fluency':c.assumptions<45?'Hidden premise failure':'Workflow ambiguity';document.getElementById('protectText').textContent=c.inspection<55?'Add an inspection checklist before submission.':c.assumptions<55?'Force the user to list assumptions before accepting output.':c.transfer<55?'Add a teach-back or redo-without-AI step.':'Keep the current WAIT checkpoints visible.'}ids.forEach(id=>document.getElementById(id).addEventListener('input',draw));document.getElementById('runBtn').onclick=draw;document.getElementById('shortcutBtn').onclick=()=>{let s={delegation:92,complexity:72,inspection:25,assumptions:22,transfer:18,pressure:88,accuracy:82,feedback:28};ids.forEach(id=>document.getElementById(id).value=s[id]);draw()};document.getElementById('waitBtn').onclick=()=>{let s={delegation:58,complexity:72,inspection:78,assumptions:76,transfer:72,pressure:55,accuracy:82,feedback:70};ids.forEach(id=>document.getElementById(id).value=s[id]);draw()};document.getElementById('resetBtn').onclick=()=>{let s={delegation:65,complexity:60,inspection:45,assumptions:40,transfer:35,pressure:70,accuracy:78,feedback:45};ids.forEach(id=>document.getElementById(id).value=s[id]);draw()};draw();
